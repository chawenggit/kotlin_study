private val arr = intArrayOf(-2, 1, -3, 4, -1, 2, 1, -5, 4)
private val arr_asc = arr.sorted()
fun main() {
    for (n in arr) print("$n ")
    println()
    for (n in arr_asc) print("$n ")
    println(arr_asc[arr_asc2.size - 2])
    println(arr.sum())
    println(arr_asc.sum())
}